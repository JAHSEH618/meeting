openapi: 3.1.0
info:
  title: Meeting Public API
  description: Meeting Intelligence Platform Public API
  version: 0.1.0
  contact:
    name: Meeting API Team
    email: api@meeting.dev
servers:
  - url: /api
security:
  - bearerAuth: []

x-api-conventions:
  successEnvelope: '#/components/schemas/ApiResponse'
  idempotency:
    requiredFor: all non-login mutating operations
    conflictStatus: 409

tags:
  - name: Auth
    description: Authentication and authorization
  - name: Users
    description: User management
  - name: Meetings
    description: Meeting resources
  - name: Files
    description: Audio file upload management
  - name: Tasks
    description: Processing task lifecycle
  - name: Transcript
    description: Transcript access and editing
  - name: Speakers
    description: Speaker profiles and matching
  - name: Minutes
    description: AI-generated meeting minutes
  - name: Items
    description: Action items, decisions, and risks
  - name: Documents
    description: Document management and indexing
  - name: RAG
    description: Retrieval-augmented generation queries
  - name: Exports
    description: Meeting export jobs
  - name: Compliance
    description: Legal holds and compliance
  - name: Admin
    description: Administrative operations including deletion jobs and break-glass

paths:
  # ── Auth ────────────────────────────────────────────────────
  /auth/login:
    post:
      operationId: login
      description: Authenticate user with credentials and obtain access token.
      security: []
      tags: [Auth]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LoginRequest'
      responses:
        '200':
          description: Login successful
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LoginResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '423':
          $ref: '#/components/responses/Locked'
        '500':
          $ref: '#/components/responses/ServerError'

  /auth/logout:
    post:
      operationId: logout
      description: Invalidate current access token and end session.
      tags: [Auth]
      parameters:
        - $ref: '#/components/parameters/XRequestId'
        - $ref: '#/components/parameters/XTraceId'
        - $ref: '#/components/parameters/IdempotencyKey'
      responses:
        '200':
          $ref: '#/components/responses/OkEmpty'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /auth/me:
    get:
      operationId: getCurrentUser
      description: Retrieve the authenticated user profile and permissions.
      tags: [Auth]
      parameters:
        - $ref: '#/components/parameters/XRequestId'
        - $ref: '#/components/parameters/XTraceId'
      responses:
        '200':
          description: Current user info
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthUserResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'

  # ── Users ──────────────────────────────────────────────────
  /users:
    get:
      operationId: listUsers
      description: List users in the current tenant with pagination.
      tags: [Users]
      parameters:
        - $ref: '#/components/parameters/XRequestId'
        - $ref: '#/components/parameters/XTraceId'
      responses:
        '200':
          $ref: '#/components/responses/OkPaginated'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
    post:
      operationId: createUser
      description: Create a new user in the current tenant.
      tags: [Users]
      parameters:
        - $ref: '#/components/parameters/XRequestId'
        - $ref: '#/components/parameters/XTraceId'
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateUserRequest'
      responses:
        '200':
          $ref: '#/components/responses/Ok'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/Unprocessable'

  /users/{userId}:
    patch:
      operationId: updateUser
      description: Update an existing user profile and roles.
      tags: [Users]
      parameters:
        - $ref: '#/components/parameters/UserId'
        - $ref: '#/components/parameters/XRequestId'
        - $ref: '#/components/parameters/XTraceId'
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateUserRequest'
      responses:
        '200':
          $ref: '#/components/responses/Ok'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/Unprocessable'

  # ── Meetings ────────────────────────────────────────────────
  /meetings:
    get:
      operationId: listMeetings
      description: List meetings accessible to the authenticated user.
      tags: [Meetings]
      parameters:
        - $ref: '#/components/parameters/XRequestId'
        - $ref: '#/components/parameters/XTraceId'
      responses:
        '200':
          description: Meeting list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MeetingListResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '500':
          $ref: '#/components/responses/ServerError'
    post:
      operationId: createMeeting
      description: Create a new meeting with participants and security level.
      tags: [Meetings]
      parameters:
        - $ref: '#/components/parameters/XRequestId'
        - $ref: '#/components/parameters/XTraceId'
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateMeetingRequest'
      responses:
        '200':
          description: Meeting created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MeetingResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/Unprocessable'

  /meetings/{meetingId}:
    get:
      operationId: getMeeting
      description: Retrieve a single meeting by ID.
      tags: [Meetings]
      parameters:
        - $ref: '#/components/parameters/MeetingId'
        - $ref: '#/components/parameters/XRequestId'
        - $ref: '#/components/parameters/XTraceId'
      responses:
        '200':
          description: Meeting detail
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MeetingResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: updateMeeting
      description: Update meeting metadata, participants, or security level.
      tags: [Meetings]
      parameters:
        - $ref: '#/components/parameters/MeetingId'
        - $ref: '#/components/parameters/XRequestId'
        - $ref: '#/components/parameters/XTraceId'
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateMeetingRequest'
      responses:
        '200':
          $ref: '#/components/responses/Ok'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
    delete:
      operationId: deleteMeeting
      description: Soft-delete a meeting with legal-hold acknowledgement.
      tags: [Meetings]
      parameters:
        - $ref: '#/components/parameters/MeetingId'
        - $ref: '#/components/parameters/XRequestId'
        - $ref: '#/components/parameters/XTraceId'
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DeleteMeetingRequest'
      responses:
        '200':
          $ref: '#/components/responses/Ok'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '423':
          $ref: '#/components/responses/Locked'

  # ── Audio Upload ────────────────────────────────────────────
  /meetings/{meetingId}/files/audio/uploads:
    post:
      operationId: createAudioUpload
      description: Initiate a multipart audio upload session for a meeting.
      tags: [Files]
      parameters:
        - $ref: '#/components/parameters/MeetingId'
        - $ref: '#/components/parameters/XRequestId'
        - $ref: '#/components/parameters/XTraceId'
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateAudioUploadRequest'
      responses:
        '200':
          description: Audio upload session created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AudioUploadSessionResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '413':
          $ref: '#/components/responses/PayloadTooLarge'
        '422':
          $ref: '#/components/responses/Unprocessable'

  /meetings/{meetingId}/files/audio/uploads/{uploadId}/parts:
    post:
      operationId: createAudioUploadPart
      description: Request a signed upload URL for one part of a multipart upload.
      tags: [Files]
      parameters:
        - $ref: '#/components/parameters/MeetingId'
        - $ref: '#/components/parameters/UploadId'
        - $ref: '#/components/parameters/XRequestId'
        - $ref: '#/components/parameters/XTraceId'
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateAudioUploadPartRequest'
      responses:
        '200':
          description: Signed upload URL for one part
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AudioUploadPartResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '410':
          $ref: '#/components/responses/Gone'
        '422':
          $ref: '#/components/responses/Unprocessable'

  /meetings/{meetingId}/files/audio/uploads/{uploadId}/complete:
    post:
      operationId: completeAudioUpload
      description: Finalize a multipart audio upload after all parts are transferred.
      tags: [Files]
      parameters:
        - $ref: '#/components/parameters/MeetingId'
        - $ref: '#/components/parameters/UploadId'
        - $ref: '#/components/parameters/XRequestId'
        - $ref: '#/components/parameters/XTraceId'
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CompleteAudioUploadRequest'
      responses:
        '200':
          description: Upload completed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AudioUploadSessionResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '410':
          $ref: '#/components/responses/Gone'
        '422':
          $ref: '#/components/responses/Unprocessable'

  /meetings/{meetingId}/files/audio/uploads/{uploadId}/abort:
    post:
      operationId: abortAudioUpload
      description: Abort an in-progress multipart audio upload session.
      tags: [Files]
      parameters:
        - $ref: '#/components/parameters/MeetingId'
        - $ref: '#/components/parameters/UploadId'
        - $ref: '#/components/parameters/XRequestId'
        - $ref: '#/components/parameters/XTraceId'
        - $ref: '#/components/parameters/IdempotencyKey'
      responses:
        '200':
          $ref: '#/components/responses/OkEmpty'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'

  /meetings/{meetingId}/files/audio/uploads/{uploadId}:
    get:
      operationId: getAudioUpload
      description: Check the status of an audio upload session.
      tags: [Files]
      parameters:
        - $ref: '#/components/parameters/MeetingId'
        - $ref: '#/components/parameters/UploadId'
        - $ref: '#/components/parameters/XRequestId'
        - $ref: '#/components/parameters/XTraceId'
      responses:
        '200':
          description: Audio upload session status
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AudioUploadSessionResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'

  # ── Processing Tasks ────────────────────────────────────────
  /meetings/{meetingId}/processing-tasks:
    post:
      operationId: createProcessingTask
      description: Start a processing pipeline for a meeting audio file.
      tags: [Tasks]
      parameters:
        - $ref: '#/components/parameters/MeetingId'
        - $ref: '#/components/parameters/XRequestId'
        - $ref: '#/components/parameters/XTraceId'
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateProcessingTaskRequest'
      responses:
        '200':
          description: Task created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProcessingTaskResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'

  /meetings/{meetingId}/processing-tasks/latest:
    get:
      operationId: getLatestMeetingTask
      description: Retrieve the latest processing task for a meeting.
      tags: [Tasks]
      parameters:
        - $ref: '#/components/parameters/MeetingId'
        - $ref: '#/components/parameters/XRequestId'
        - $ref: '#/components/parameters/XTraceId'
      responses:
        '200':
          description: Latest task
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProcessingTaskResponse'
        '404':
          $ref: '#/components/responses/NotFound'

  /processing-tasks/{taskId}:
    get:
      operationId: getProcessingTask
      description: Retrieve a processing task by ID.
      tags: [Tasks]
      parameters:
        - $ref: '#/components/parameters/TaskId'
        - $ref: '#/components/parameters/XRequestId'
        - $ref: '#/components/parameters/XTraceId'
      responses:
        '200':
          description: Task detail
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProcessingTaskResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'

  /processing-tasks/{taskId}/events:
    get:
      operationId: streamProcessingTaskEvents
      description: Subscribe to SSE events for a processing task.
      tags: [Tasks]
      parameters:
        - $ref: '#/components/parameters/TaskId'
        - name: Last-Event-Id
          in: header
          required: false
          schema:
            type: string
      responses:
        '200':
          description: SSE task events. Each event data matches TaskEvent.
          content:
            text/event-stream:
              schema:
                $ref: '#/components/schemas/TaskEvent'
        '404':
          $ref: '#/components/responses/NotFound'
        '410':
          $ref: '#/components/responses/Gone'

  /processing-tasks/{taskId}/retry:
    post:
      operationId: retryProcessingTask
      description: Retry a failed or orphaned processing task.
      tags: [Tasks]
      parameters:
        - $ref: '#/components/parameters/TaskId'
        - $ref: '#/components/parameters/XRequestId'
        - $ref: '#/components/parameters/XTraceId'
        - $ref: '#/components/parameters/IdempotencyKey'
      responses:
        '200':
          $ref: '#/components/responses/Ok'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'

  /processing-tasks/{taskId}/cancel:
    post:
      operationId: cancelProcessingTask
      description: Request cancellation of a running processing task.
      tags: [Tasks]
      parameters:
        - $ref: '#/components/parameters/TaskId'
        - $ref: '#/components/parameters/XRequestId'
        - $ref: '#/components/parameters/XTraceId'
        - $ref: '#/components/parameters/IdempotencyKey'
      responses:
        '200':
          $ref: '#/components/responses/Ok'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'

  # ── Transcript ──────────────────────────────────────────────
  /meetings/{meetingId}/transcript:
    get:
      operationId: getTranscript
      description: Retrieve the transcript with segments for a meeting.
      tags: [Transcript]
      parameters:
        - $ref: '#/components/parameters/MeetingId'
        - $ref: '#/components/parameters/XRequestId'
        - $ref: '#/components/parameters/XTraceId'
      responses:
        '200':
          description: Transcript with segments
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TranscriptResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'

  /meetings/{meetingId}/transcript/segments/{segmentId}:
    patch:
      operationId: updateTranscriptSegment
      description: Edit a single transcript segment text.
      tags: [Transcript]
      parameters:
        - $ref: '#/components/parameters/MeetingId'
        - $ref: '#/components/parameters/SegmentId'
        - $ref: '#/components/parameters/XRequestId'
        - $ref: '#/components/parameters/XTraceId'
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateTranscriptSegmentRequest'
      responses:
        '200':
          $ref: '#/components/responses/Ok'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/Unprocessable'

  /meetings/{meetingId}/transcript/regenerate:
    post:
      operationId: regenerateTranscriptDependents
      description: Regenerate downstream artefacts affected by transcript edits.
      tags: [Transcript]
      parameters:
        - $ref: '#/components/parameters/MeetingId'
        - $ref: '#/components/parameters/XRequestId'
        - $ref: '#/components/parameters/XTraceId'
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RegenerateTranscriptDependentsRequest'
      responses:
        '200':
          $ref: '#/components/responses/Ok'

  # ── Speaker Profiles ────────────────────────────────────────
  /speaker-profiles:
    get:
      operationId: listSpeakerProfiles
      description: List speaker profiles in the current tenant.
      tags: [Speakers]
      parameters:
        - $ref: '#/components/parameters/XRequestId'
        - $ref: '#/components/parameters/XTraceId'
      responses:
        '200':
          description: Speaker profile list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SpeakerProfileListResponse'
    post:
      operationId: createSpeakerProfile
      description: Create a new speaker profile with consent reference.
      tags: [Speakers]
      parameters:
        - $ref: '#/components/parameters/XRequestId'
        - $ref: '#/components/parameters/XTraceId'
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateSpeakerProfileRequest'
      responses:
        '200':
          $ref: '#/components/responses/Ok'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/Unprocessable'

  /speaker-profiles/{profileId}:
    get:
      operationId: getSpeakerProfile
      description: Retrieve a speaker profile by ID.
      tags: [Speakers]
      parameters:
        - $ref: '#/components/parameters/ProfileId'
        - $ref: '#/components/parameters/XRequestId'
        - $ref: '#/components/parameters/XTraceId'
      responses:
        '200':
          $ref: '#/components/responses/Ok'
        '404':
          $ref: '#/components/responses/NotFound'
    delete:
      operationId: deleteSpeakerProfile
      description: Delete a speaker profile and its enrollments.
      tags: [Speakers]
      parameters:
        - $ref: '#/components/parameters/ProfileId'
        - $ref: '#/components/parameters/XRequestId'
        - $ref: '#/components/parameters/XTraceId'
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DeleteSpeakerProfileRequest'
      responses:
        '200':
          $ref: '#/components/responses/Ok'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'

  /speaker-profiles/{profileId}/enrollments:
    post:
      operationId: createSpeakerEnrollment
      description: Add a voice enrollment to a speaker profile.
      tags: [Speakers]
      parameters:
        - $ref: '#/components/parameters/ProfileId'
        - $ref: '#/components/parameters/XRequestId'
        - $ref: '#/components/parameters/XTraceId'
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateSpeakerEnrollmentRequest'
      responses:
        '200':
          $ref: '#/components/responses/Ok'

  /speaker-profiles/{profileId}/revoke:
    post:
      operationId: revokeSpeakerProfile
      description: Revoke a speaker profile, disabling future matching.
      tags: [Speakers]
      parameters:
        - $ref: '#/components/parameters/ProfileId'
        - $ref: '#/components/parameters/XRequestId'
        - $ref: '#/components/parameters/XTraceId'
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RevokeSpeakerProfileRequest'
      responses:
        '200':
          $ref: '#/components/responses/Ok'
        '409':
          $ref: '#/components/responses/Conflict'

  /meetings/{meetingId}/speakers:
    get:
      operationId: listMeetingSpeakers
      description: List speaker labels and match candidates for a meeting.
      tags: [Speakers]
      parameters:
        - $ref: '#/components/parameters/MeetingId'
        - $ref: '#/components/parameters/XRequestId'
        - $ref: '#/components/parameters/XTraceId'
      responses:
        '200':
          description: Meeting speaker labels and candidates
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MeetingSpeakerListResponse'
        '404':
          $ref: '#/components/responses/NotFound'

  /meetings/{meetingId}/speakers/{speakerLabel}/confirm:
    post:
      operationId: confirmMeetingSpeaker
      description: Confirm a speaker candidate match for a label.
      tags: [Speakers]
      parameters:
        - $ref: '#/components/parameters/MeetingId'
        - $ref: '#/components/parameters/SpeakerLabel'
        - $ref: '#/components/parameters/XRequestId'
        - $ref: '#/components/parameters/XTraceId'
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ConfirmMeetingSpeakerRequest'
      responses:
        '200':
          $ref: '#/components/responses/Ok'

  /meetings/{meetingId}/speakers/{speakerLabel}/reject:
    post:
      operationId: rejectMeetingSpeaker
      description: Reject a speaker candidate match for a label.
      tags: [Speakers]
      parameters:
        - $ref: '#/components/parameters/MeetingId'
        - $ref: '#/components/parameters/SpeakerLabel'
        - $ref: '#/components/parameters/XRequestId'
        - $ref: '#/components/parameters/XTraceId'
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RejectMeetingSpeakerRequest'
      responses:
        '200':
          $ref: '#/components/responses/Ok'

  # ── Minutes ─────────────────────────────────────────────────
  /meetings/{meetingId}/minutes:
    get:
      operationId: getMinutes
      description: Retrieve AI-generated minutes for a meeting.
      tags: [Minutes]
      parameters:
        - $ref: '#/components/parameters/MeetingId'
        - $ref: '#/components/parameters/XRequestId'
        - $ref: '#/components/parameters/XTraceId'
      responses:
        '200':
          description: Meeting minutes
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MinutesResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'

  /meetings/{meetingId}/minutes/regenerate:
    post:
      operationId: regenerateMinutes
      description: Regenerate AI minutes from the current transcript version.
      tags: [Minutes]
      parameters:
        - $ref: '#/components/parameters/MeetingId'
        - $ref: '#/components/parameters/XRequestId'
        - $ref: '#/components/parameters/XTraceId'
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RegenerateMinutesRequest'
      responses:
        '200':
          $ref: '#/components/responses/Ok'

  # ── Action Items ────────────────────────────────────────────
  /meetings/{meetingId}/action-items:
    get:
      operationId: listActionItems
      description: List action items extracted from a meeting.
      tags: [Items]
      parameters:
        - $ref: '#/components/parameters/MeetingId'
        - $ref: '#/components/parameters/XRequestId'
        - $ref: '#/components/parameters/XTraceId'
      responses:
        '200':
          $ref: '#/components/responses/OkPaginated'
  /meetings/{meetingId}/action-items/{itemId}:
    patch:
      operationId: updateActionItem
      description: Update an action item title, assignee, due date, or status.
      tags: [Items]
      parameters:
        - $ref: '#/components/parameters/MeetingId'
        - $ref: '#/components/parameters/ItemId'
        - $ref: '#/components/parameters/XRequestId'
        - $ref: '#/components/parameters/XTraceId'
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateActionItemRequest'
      responses:
        '200':
          $ref: '#/components/responses/Ok'
  /meetings/{meetingId}/action-items/{itemId}/accept:
    post:
      operationId: acceptActionItem
      description: Accept an AI-suggested action item as confirmed.
      tags: [Items]
      parameters:
        - $ref: '#/components/parameters/MeetingId'
        - $ref: '#/components/parameters/ItemId'
        - $ref: '#/components/parameters/XRequestId'
        - $ref: '#/components/parameters/XTraceId'
        - $ref: '#/components/parameters/IdempotencyKey'
      responses:
        '200':
          $ref: '#/components/responses/Ok'
  /meetings/{meetingId}/action-items/{itemId}/reject:
    post:
      operationId: rejectActionItem
      description: Reject an AI-suggested action item.
      tags: [Items]
      parameters:
        - $ref: '#/components/parameters/MeetingId'
        - $ref: '#/components/parameters/ItemId'
        - $ref: '#/components/parameters/XRequestId'
        - $ref: '#/components/parameters/XTraceId'
        - $ref: '#/components/parameters/IdempotencyKey'
      responses:
        '200':
          $ref: '#/components/responses/Ok'

  /meetings/{meetingId}/decisions:
    get:
      operationId: listDecisions
      description: List decisions extracted from a meeting.
      tags: [Items]
      parameters:
        - $ref: '#/components/parameters/MeetingId'
        - $ref: '#/components/parameters/XRequestId'
        - $ref: '#/components/parameters/XTraceId'
      responses:
        '200':
          $ref: '#/components/responses/OkPaginated'
  /meetings/{meetingId}/risks:
    get:
      operationId: listRisks
      description: List risks extracted from a meeting.
      tags: [Items]
      parameters:
        - $ref: '#/components/parameters/MeetingId'
        - $ref: '#/components/parameters/XRequestId'
        - $ref: '#/components/parameters/XTraceId'
      responses:
        '200':
          $ref: '#/components/responses/OkPaginated'

  # ── Documents ───────────────────────────────────────────────
  /documents:
    get:
      operationId: listDocuments
      description: List documents accessible to the authenticated user.
      tags: [Documents]
      parameters:
        - $ref: '#/components/parameters/XRequestId'
        - $ref: '#/components/parameters/XTraceId'
      responses:
        '200':
          description: Documents readable by the caller
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DocumentListResponse'
    post:
      operationId: createDocument
      description: Register a new document for indexing.
      tags: [Documents]
      parameters:
        - $ref: '#/components/parameters/XRequestId'
        - $ref: '#/components/parameters/XTraceId'
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateDocumentRequest'
      responses:
        '200':
          description: Document registered
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DocumentResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '422':
          $ref: '#/components/responses/Unprocessable'
  /documents/{documentId}:
    get:
      operationId: getDocument
      description: Retrieve a document by ID.
      tags: [Documents]
      parameters:
        - $ref: '#/components/parameters/DocumentId'
        - $ref: '#/components/parameters/XRequestId'
        - $ref: '#/components/parameters/XTraceId'
      responses:
        '200':
          description: Document detail
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DocumentResponse'
        '404':
          $ref: '#/components/responses/NotFound'
    delete:
      operationId: deleteDocument
      description: Delete a document and its indexed chunks.
      tags: [Documents]
      parameters:
        - $ref: '#/components/parameters/DocumentId'
        - $ref: '#/components/parameters/XRequestId'
        - $ref: '#/components/parameters/XTraceId'
        - $ref: '#/components/parameters/IdempotencyKey'
      responses:
        '200':
          $ref: '#/components/responses/Ok'
        '404':
          $ref: '#/components/responses/NotFound'
        '423':
          $ref: '#/components/responses/Locked'
  /documents/{documentId}/reindex:
    post:
      operationId: reindexDocument
      description: Reindex a document into the RAG vector store.
      tags: [Documents]
      parameters:
        - $ref: '#/components/parameters/DocumentId'
        - $ref: '#/components/parameters/XRequestId'
        - $ref: '#/components/parameters/XTraceId'
        - $ref: '#/components/parameters/IdempotencyKey'
      responses:
        '200':
          description: Document reindex accepted; returns the updated document state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DocumentResponse'
        '404':
          $ref: '#/components/responses/NotFound'

  # ── RAG ─────────────────────────────────────────────────────
  /rag/query:
    post:
      operationId: queryRag
      description: Submit a RAG query and receive an answer with citations.
      tags: [RAG]
      parameters:
        - $ref: '#/components/parameters/XRequestId'
        - $ref: '#/components/parameters/XTraceId'
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RagQueryRequest'
      responses:
        '200':
          description: RAG answer with citations
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RagAnswerResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/Unprocessable'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /rag/reindex/meetings/{meetingId}:
    post:
      operationId: reindexMeetingRag
      description: Reindex a meeting transcript and minutes into the RAG vector store.
      tags: [RAG]
      parameters:
        - $ref: '#/components/parameters/MeetingId'
        - $ref: '#/components/parameters/XRequestId'
        - $ref: '#/components/parameters/XTraceId'
        - $ref: '#/components/parameters/IdempotencyKey'
      responses:
        '200':
          $ref: '#/components/responses/Ok'

  /rag/reindex/documents/{documentId}:
    post:
      operationId: reindexDocumentRag
      description: Reindex a document into the RAG vector store.
      tags: [RAG]
      parameters:
        - $ref: '#/components/parameters/DocumentId'
        - $ref: '#/components/parameters/XRequestId'
        - $ref: '#/components/parameters/XTraceId'
        - $ref: '#/components/parameters/IdempotencyKey'
      responses:
        '200':
          $ref: '#/components/responses/Ok'

  # ── Export ──────────────────────────────────────────────────
  /meetings/{meetingId}/exports:
    get:
      operationId: listMeetingExports
      description: List export jobs for a meeting.
      tags: [Exports]
      parameters:
        - $ref: '#/components/parameters/MeetingId'
        - $ref: '#/components/parameters/XRequestId'
        - $ref: '#/components/parameters/XTraceId'
      responses:
        '200':
          $ref: '#/components/responses/OkPaginated'
    post:
      operationId: createMeetingExport
      description: Create a new export job for a meeting.
      tags: [Exports]
      parameters:
        - $ref: '#/components/parameters/MeetingId'
        - $ref: '#/components/parameters/XRequestId'
        - $ref: '#/components/parameters/XTraceId'
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateExportRequest'
      responses:
        '200':
          description: Export job created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExportJobResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '422':
          $ref: '#/components/responses/Unprocessable'

  /exports/{exportId}:
    get:
      operationId: getExport
      description: Retrieve an export job status and download URL.
      tags: [Exports]
      parameters:
        - $ref: '#/components/parameters/ExportId'
        - $ref: '#/components/parameters/XRequestId'
        - $ref: '#/components/parameters/XTraceId'
      responses:
        '200':
          description: Export job status + download URL
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExportJobResponse'
        '404':
          $ref: '#/components/responses/NotFound'

  /exports/{exportId}/cancel:
    post:
      operationId: cancelExport
      description: Cancel an in-progress export job.
      tags: [Exports]
      parameters:
        - $ref: '#/components/parameters/ExportId'
        - $ref: '#/components/parameters/XRequestId'
        - $ref: '#/components/parameters/XTraceId'
        - $ref: '#/components/parameters/IdempotencyKey'
      responses:
        '200':
          $ref: '#/components/responses/Ok'

  /exports/{exportId}/revoke-link:
    post:
      operationId: revokeExportLink
      description: Revoke the download URL of a completed export.
      tags: [Exports]
      parameters:
        - $ref: '#/components/parameters/ExportId'
        - $ref: '#/components/parameters/XRequestId'
        - $ref: '#/components/parameters/XTraceId'
        - $ref: '#/components/parameters/IdempotencyKey'
      responses:
        '200':
          $ref: '#/components/responses/Ok'

  # ── Compliance / Admin ──────────────────────────────────────
  /legal-holds:
    get:
      operationId: listLegalHolds
      description: List legal holds in the current tenant.
      tags: [Compliance]
      parameters:
        - $ref: '#/components/parameters/XRequestId'
        - $ref: '#/components/parameters/XTraceId'
      responses:
        '200':
          $ref: '#/components/responses/OkPaginated'
    post:
      operationId: createLegalHold
      description: Place a legal hold on one or more entities.
      tags: [Compliance]
      parameters:
        - $ref: '#/components/parameters/XRequestId'
        - $ref: '#/components/parameters/XTraceId'
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateLegalHoldRequest'
      responses:
        '201':
          description: Legal hold created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LegalHoldResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '409':
          $ref: '#/components/responses/Conflict'

  /legal-holds/{legalHoldId}:
    get:
      operationId: getLegalHold
      description: Retrieve a legal hold by ID.
      tags: [Compliance]
      parameters:
        - $ref: '#/components/parameters/LegalHoldId'
        - $ref: '#/components/parameters/XRequestId'
        - $ref: '#/components/parameters/XTraceId'
      responses:
        '200':
          $ref: '#/components/responses/Ok'
    delete:
      operationId: deleteLegalHold
      description: Delete a legal hold (alias for release).
      tags: [Compliance]
      parameters:
        - $ref: '#/components/parameters/LegalHoldId'
        - $ref: '#/components/parameters/XRequestId'
        - $ref: '#/components/parameters/XTraceId'
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReleaseLegalHoldRequest'
      responses:
        '200':
          $ref: '#/components/responses/Ok'

  /legal-holds/{legalHoldId}/release:
    put:
      operationId: releaseLegalHold
      description: Release a legal hold and unfreeze affected entities.
      tags: [Compliance]
      parameters:
        - $ref: '#/components/parameters/LegalHoldId'
        - $ref: '#/components/parameters/XRequestId'
        - $ref: '#/components/parameters/XTraceId'
        - $ref: '#/components/parameters/IdempotencyKey'
      responses:
        '200':
          $ref: '#/components/responses/Ok'

  /admin/deletion-jobs:
    get:
      operationId: listDeletionJobs
      description: List data deletion jobs in the current tenant.
      tags: [Admin]
      parameters:
        - $ref: '#/components/parameters/XRequestId'
        - $ref: '#/components/parameters/XTraceId'
      responses:
        '200':
          $ref: '#/components/responses/OkPaginated'
    post:
      operationId: createDeletionJob
      description: Create a data deletion job for specified entities.
      tags: [Admin]
      parameters:
        - $ref: '#/components/parameters/XRequestId'
        - $ref: '#/components/parameters/XTraceId'
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateDeletionJobRequest'
      responses:
        '200':
          $ref: '#/components/responses/Ok'

  /admin/deletion-jobs/{jobId}:
    get:
      operationId: getDeletionJob
      description: Retrieve a deletion job by ID.
      tags: [Admin]
      parameters:
        - $ref: '#/components/parameters/JobId'
        - $ref: '#/components/parameters/XRequestId'
        - $ref: '#/components/parameters/XTraceId'
      responses:
        '200':
          $ref: '#/components/responses/Ok'

  /admin/deletion-jobs/{jobId}/certificate:
    get:
      operationId: getDeletionCertificate
      description: Retrieve the deletion certificate for a completed job.
      tags: [Admin]
      parameters:
        - $ref: '#/components/parameters/JobId'
        - $ref: '#/components/parameters/XRequestId'
        - $ref: '#/components/parameters/XTraceId'
      responses:
        '200':
          $ref: '#/components/responses/Ok'

  /admin/break-glass/requests:
    get:
      operationId: listBreakGlassRequests
      description: List break-glass access requests.
      tags: [Admin]
      parameters:
        - $ref: '#/components/parameters/XRequestId'
        - $ref: '#/components/parameters/XTraceId'
      responses:
        '200':
          $ref: '#/components/responses/OkPaginated'
    post:
      operationId: createBreakGlassRequest
      description: Request temporary elevated access to restricted data.
      tags: [Admin]
      parameters:
        - $ref: '#/components/parameters/XRequestId'
        - $ref: '#/components/parameters/XTraceId'
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateBreakGlassRequest'
      responses:
        '200':
          $ref: '#/components/responses/Ok'

  /admin/break-glass/requests/{requestId}/approve:
    post:
      operationId: approveBreakGlassRequest
      description: Approve a pending break-glass access request.
      tags: [Admin]
      parameters:
        - $ref: '#/components/parameters/BreakGlassRequestId'
        - $ref: '#/components/parameters/XRequestId'
        - $ref: '#/components/parameters/XTraceId'
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ApproveBreakGlassRequest'
      responses:
        '200':
          $ref: '#/components/responses/Ok'

  /admin/break-glass/requests/{requestId}/reject:
    post:
      operationId: rejectBreakGlassRequest
      description: Reject a pending break-glass access request.
      tags: [Admin]
      parameters:
        - $ref: '#/components/parameters/BreakGlassRequestId'
        - $ref: '#/components/parameters/XRequestId'
        - $ref: '#/components/parameters/XTraceId'
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RejectBreakGlassRequest'
      responses:
        '200':
          $ref: '#/components/responses/Ok'

  /admin/break-glass/audit:
    get:
      operationId: listBreakGlassAudit
      description: List the break-glass access audit trail.
      tags: [Admin]
      parameters:
        - $ref: '#/components/parameters/XRequestId'
        - $ref: '#/components/parameters/XTraceId'
      responses:
        '200':
          $ref: '#/components/responses/OkPaginated'

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

  parameters:
    UserId:
      name: userId
      in: path
      required: true
      schema: {type: string}
    MeetingId:
      name: meetingId
      in: path
      required: true
      schema: {type: string}
    UploadId:
      name: uploadId
      in: path
      required: true
      schema: {type: string}
    TaskId:
      name: taskId
      in: path
      required: true
      schema: {type: string}
    XRequestId:
      name: X-Request-Id
      in: header
      required: true
      schema: {type: string, minLength: 1}
    XTraceId:
      name: X-Trace-Id
      in: header
      required: true
      schema: {type: string, minLength: 1}
    IdempotencyKey:
      name: Idempotency-Key
      in: header
      required: true
      schema: {type: string, minLength: 1}
    SegmentId:
      name: segmentId
      in: path
      required: true
      schema: {type: string}
    ProfileId:
      name: profileId
      in: path
      required: true
      schema: {type: string}
    SpeakerLabel:
      name: speakerLabel
      in: path
      required: true
      schema: {type: string}
    DocumentId:
      name: documentId
      in: path
      required: true
      schema: {type: string}
    ExportId:
      name: exportId
      in: path
      required: true
      schema: {type: string}
    ItemId:
      name: itemId
      in: path
      required: true
      schema: {type: string}
    LegalHoldId:
      name: legalHoldId
      in: path
      required: true
      schema: {type: string}
    JobId:
      name: jobId
      in: path
      required: true
      schema: {type: string}
    BreakGlassRequestId:
      name: requestId
      in: path
      required: true
      schema: {type: string}

  responses:
    Ok:
      description: Standard success response
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ApiResponse'
    OkEmpty:
      description: Success with no data body
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ApiResponse'
    OkPaginated:
      description: Paginated success response
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ApiResponse'
    BadRequest:
      description: Invalid request parameters
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ApiResponse'
    Unauthorized:
      description: Authentication required
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ApiResponse'
    Forbidden:
      description: Permission denied
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ApiResponse'
    NotFound:
      description: Resource not found
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ApiResponse'
    Conflict:
      description: Version or state conflict
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ApiResponse'
    Unprocessable:
      description: Semantic validation failed
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ApiResponse'
    Locked:
      description: Resource locked (legal hold, account lockout)
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ApiResponse'
    PayloadTooLarge:
      description: Upload exceeds size limit
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ApiResponse'
    TooManyRequests:
      description: Rate limit exceeded
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ApiResponse'
    Gone:
      description: SSE event window expired
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ApiResponse'
    ServerError:
      description: Internal server error
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ApiResponse'

  schemas:
    # ── Envelope ───────────────────────────────────────────────
    ApiResponse:
      type: object
      required: [success, data, error, requestId, traceId]
      properties:
        success: {type: boolean}
        data: {}
        error:
          anyOf:
            - $ref: '#/components/schemas/ErrorInfo'
            - type: 'null'
        requestId: {type: string}
        traceId: {type: string}

    ErrorInfo:
      type: object
      required: [code, message, retryable]
      properties:
        code: {type: string}
        message: {type: string}
        retryable: {type: boolean}
        details:
          type: object
          additionalProperties: true

    # ── User Requests ─────────────────────────────────────────
    CreateUserRequest:
      type: object
      required: [username, displayName, roles]
      properties:
        username: {type: string, minLength: 1}
        displayName: {type: string, minLength: 1}
        email: {type: [string, 'null'], format: email}
        personId: {type: [string, 'null']}
        roles:
          type: array
          minItems: 1
          items: {type: string}

    UpdateUserRequest:
      type: object
      minProperties: 1
      properties:
        displayName: {type: string}
        email: {type: [string, 'null'], format: email}
        enabled: {type: boolean}
        roles:
          type: array
          items: {type: string}
        expectedVersion: {type: integer, minimum: 0}

    # ── Auth ───────────────────────────────────────────────────
    LoginRequest:
      type: object
      required: [username, password]
      properties:
        username: {type: string}
        password: {type: string}

    LoginResponse:
      type: object
      required: [success, data, error, requestId, traceId]
      properties:
        success: {type: boolean}
        data:
          type: object
          required: [accessToken, expiresAt, user]
          properties:
            accessToken: {type: string}
            expiresAt: {type: string, format: date-time}
            user:
              $ref: '#/components/schemas/AuthUser'
        error:
          anyOf:
            - $ref: '#/components/schemas/ErrorInfo'
            - type: 'null'
        requestId: {type: string}
        traceId: {type: string}

    AuthUserResponse:
      type: object
      required: [success, data, error, requestId, traceId]
      properties:
        success: {type: boolean}
        data:
          $ref: '#/components/schemas/AuthUser'
        error:
          anyOf:
            - $ref: '#/components/schemas/ErrorInfo'
            - type: 'null'
        requestId: {type: string}
        traceId: {type: string}

    AuthUser:
      type: object
      required: [userId, tenantId, displayName, roles, permissions]
      properties:
        userId: {type: string}
        tenantId: {type: string}
        personId: {type: string}
        displayName: {type: string}
        roles:
          type: array
          items: {type: string}
        permissions:
          type: array
          items: {type: string}

    # ── Meeting ────────────────────────────────────────────────
    CreateMeetingRequest:
      type: object
      required: [title, securityLevel]
      properties:
        title: {type: string}
        scheduledStartAt:
          type: string
          format: date-time
        securityLevel:
          $ref: '#/components/schemas/SecurityLevel'
        language: {type: string, default: 'zh'}
        participants:
          type: array
          items:
            type: object
            required: [personId, displayName, role]
            properties:
              personId: {type: string}
              displayName: {type: string}
              role: {type: string}

    UpdateMeetingRequest:
      type: object
      minProperties: 1
      properties:
        title: {type: string, minLength: 1}
        scheduledStartAt: {type: [string, 'null'], format: date-time}
        participants:
          type: array
          items:
            type: object
            required: [personId, displayName, role]
            properties:
              personId: {type: string}
              displayName: {type: string}
              role: {type: string}
        expectedVersion: {type: integer, minimum: 0}

    MeetingResponse:
      type: object
      required: [success, data, error, requestId, traceId]
      properties:
        success: {type: boolean}
        data:
          $ref: '#/components/schemas/Meeting'
        error:
          anyOf:
            - $ref: '#/components/schemas/ErrorInfo'
            - type: 'null'
        requestId: {type: string}
        traceId: {type: string}

    MeetingListResponse:
      type: object
      required: [success, data, error, requestId, traceId]
      properties:
        success: {type: boolean}
        data:
          type: object
          properties:
            items:
              type: array
              items:
                $ref: '#/components/schemas/Meeting'
            page:
              $ref: '#/components/schemas/PageInfo'
        error:
          anyOf:
            - $ref: '#/components/schemas/ErrorInfo'
            - type: 'null'
        requestId: {type: string}
        traceId: {type: string}

    Meeting:
      type: object
      required: [meetingId, tenantId, title, securityLevel, status, language, transcriptVersion, minutesVersion, createdAt]
      properties:
        meetingId: {type: string}
        tenantId: {type: string}
        title: {type: string}
        scheduledStartAt: {type: string, format: date-time}
        securityLevel:
          $ref: '#/components/schemas/SecurityLevel'
        status:
          $ref: '#/components/schemas/MeetingStatus'
        language: {type: string}
        transcriptVersion: {type: integer, minimum: 0}
        minutesVersion: {type: integer, minimum: 0}
        createdAt: {type: string, format: date-time}

    PageInfo:
      type: object
      properties:
        cursor: {type: string}
        hasMore: {type: boolean}
        limit: {type: integer}

    # ── Audio Upload ──────────────────────────────────────────
    CreateAudioUploadRequest:
      type: object
      required: [fileName, contentType, fileSizeBytes, fileSha256]
      additionalProperties: false
      properties:
        fileName: {type: string, minLength: 1}
        contentType: {type: string, minLength: 1}
        fileSizeBytes: {type: integer, minimum: 1}
        fileSha256:
          type: string
          pattern: '^[0-9a-f]{64}$'
        partSizeBytes:
          type: integer
          minimum: 5242880
          default: 8388608

    CreateAudioUploadPartRequest:
      type: object
      required: [partNumber, sizeBytes, partSha256]
      additionalProperties: false
      properties:
        partNumber: {type: integer, minimum: 1, maximum: 10000}
        sizeBytes:
          type: integer
          minimum: 1
          maximum: 8388608
        partSha256:
          type: string
          pattern: '^[0-9a-f]{64}$'

    CompleteAudioUploadRequest:
      type: object
      required: [fileSha256, parts]
      additionalProperties: false
      properties:
        fileSha256:
          type: string
          pattern: '^[0-9a-f]{64}$'
        durationMs: {type: [integer, 'null'], minimum: 1}
        parts:
          type: array
          minItems: 1
          maxItems: 10000
          items:
            type: object
            required: [partNumber, partSha256, etag]
            additionalProperties: false
            properties:
              partNumber: {type: integer, minimum: 1, maximum: 10000}
              partSha256:
                type: string
                pattern: '^[0-9a-f]{64}$'
              etag: {type: string, minLength: 1}

    AudioUploadSessionResponse:
      type: object
      required: [success, data, error, requestId, traceId]
      properties:
        success: {type: boolean}
        data:
          $ref: '#/components/schemas/AudioUploadSession'
        error:
          anyOf:
            - $ref: '#/components/schemas/ErrorInfo'
            - type: 'null'
        requestId: {type: string}
        traceId: {type: string}

    AudioUploadSession:
      type: object
      required: [uploadId, meetingId, uploadStatus, expiresAt, partSizeBytes, maxPartCount, contentType, fileName, fileSizeBytes, fileSha256, parts]
      additionalProperties: false
      properties:
        uploadId: {type: string}
        meetingId: {type: string}
        uploadStatus:
          $ref: '#/components/schemas/AudioUploadStatus'
        expiresAt: {type: string, format: date-time}
        partSizeBytes: {type: integer, minimum: 5242880, default: 8388608}
        maxPartCount: {type: integer, minimum: 1, maximum: 10000, default: 10000}
        objectKey: {type: [string, 'null']}
        bucket: {type: [string, 'null']}
        contentType: {type: string}
        fileName: {type: string}
        fileSizeBytes: {type: integer, minimum: 1}
        fileSha256:
          type: string
          pattern: '^[0-9a-f]{64}$'
        fileId: {type: [string, 'null']}
        parts:
          type: array
          items:
            $ref: '#/components/schemas/AudioUploadPart'

    AudioUploadPartResponse:
      type: object
      required: [success, data, error, requestId, traceId]
      properties:
        success: {type: boolean}
        data:
          type: object
          required: [uploadId, partNumber, partSha256, uploadUrl, expiresAt, headers]
          additionalProperties: false
          properties:
            uploadId: {type: string}
            partNumber: {type: integer, minimum: 1, maximum: 10000}
            partSha256:
              type: string
              pattern: '^[0-9a-f]{64}$'
            etag: {type: [string, 'null']}
            uploadUrl: {type: string}
            expiresAt: {type: string, format: date-time}
            headers:
              type: object
              additionalProperties: {type: string}
        error:
          anyOf:
            - $ref: '#/components/schemas/ErrorInfo'
            - type: 'null'
        requestId: {type: string}
        traceId: {type: string}

    AudioUploadPart:
      type: object
      required: [partNumber, partSha256, sizeBytes, uploadStatus]
      additionalProperties: false
      properties:
        partNumber: {type: integer, minimum: 1, maximum: 10000}
        partSha256:
          type: string
          pattern: '^[0-9a-f]{64}$'
        etag: {type: [string, 'null']}
        sizeBytes: {type: integer, minimum: 1}
        uploadStatus:
          $ref: '#/components/schemas/AudioUploadStatus'
        uploadedAt: {type: [string, 'null'], format: date-time}

    # ── Processing Task ────────────────────────────────────────
    CreateProcessingTaskRequest:
      type: object
      required: [taskType]
      properties:
        taskType:
          type: string
          enum: [MEETING_FULL_PIPELINE]
        options:
          type: object
          additionalProperties: true
        expectedInputVersion:
          type: object
          additionalProperties: true

    ProcessingTaskResponse:
      type: object
      required: [success, data, error, requestId, traceId]
      properties:
        success: {type: boolean}
        data:
          $ref: '#/components/schemas/ProcessingTask'
        error:
          anyOf:
            - $ref: '#/components/schemas/ErrorInfo'
            - type: 'null'
        requestId: {type: string}
        traceId: {type: string}

    ProcessingTask:
      type: object
      required: [taskId, meetingId, status, phase, attemptNo, steps]
      properties:
        taskId: {type: string}
        meetingId: {type: [string, 'null']}
        status:
          $ref: '#/components/schemas/ProcessingTaskStatus'
        phase:
          $ref: '#/components/schemas/ProcessingTaskPhase'
        attemptNo: {type: integer}
        currentStep: {type: [string, 'null']}
        lastErrorCode: {type: [string, 'null']}
        retryable: {type: boolean}
        estimatedWaitSeconds: {type: integer}
        steps:
          type: array
          items:
            $ref: '#/components/schemas/ProcessingTaskStep'

    ProcessingTaskStep:
      type: object
      required: [stepName, status, progress, source]
      properties:
        stepName:
          $ref: '#/components/schemas/ProcessingStep'
        status:
          $ref: '#/components/schemas/StepStatus'
        progress:
          type: integer
          minimum: 0
          maximum: 100
        startedAt: {type: [string, 'null'], format: date-time}
        finishedAt: {type: [string, 'null'], format: date-time}
        heartbeatAt: {type: [string, 'null'], format: date-time}
        attemptNo: {type: [integer, 'null']}
        leaseOwner: {type: [string, 'null']}
        workerId: {type: [string, 'null']}
        retryable: {type: [boolean, 'null']}
        source:
          $ref: '#/components/schemas/ProcessingStepUpdateSource'

    TaskEvent:
      type: object
      required: [eventId, sequenceNo, eventType, taskId, status, emittedAt]
      properties:
        eventId: {type: string}
        sequenceNo: {type: integer}
        eventType:
          type: string
          enum: [TASK_SNAPSHOT, TASK_STARTED, TASK_STEP_UPDATED, TASK_HEARTBEAT, TRANSCRIPT_READY, TASK_FAILED, TASK_COMPLETED, TASK_CANCELLED]
        taskId: {type: string}
        meetingId: {type: [string, 'null']}
        stepName: {type: [string, 'null']}
        status: {type: string}
        phase:
          anyOf:
            - $ref: '#/components/schemas/ProcessingTaskPhase'
            - type: 'null'
        progress: {type: [integer, 'null']}
        retryable: {type: [boolean, 'null']}
        errorCode: {type: [string, 'null']}
        emittedAt: {type: string, format: date-time}
        attemptNo: {type: [integer, 'null']}
        transcriptVersion: {type: [integer, 'null']}
        artifactManifestId: {type: [string, 'null']}
        completedSteps:
          type: [array, 'null']
          items: {type: string}
        steps:
          type: [array, 'null']
          items:
            $ref: '#/components/schemas/ProcessingTaskStep'
        leaseExpiresAt: {type: [string, 'null'], format: date-time}

    # ── Transcript ─────────────────────────────────────────────
    UpdateTranscriptSegmentRequest:
      type: object
      required: [currentText, expectedTranscriptVersion]
      properties:
        currentText: {type: string}
        expectedTranscriptVersion: {type: integer, minimum: 0}

    RegenerateTranscriptDependentsRequest:
      type: object
      required: [regenerateScope, expectedTranscriptVersion]
      properties:
        regenerateScope:
          type: array
          minItems: 1
          items:
            type: string
            enum: [SUMMARY, ACTION_ITEMS, DECISIONS, RISKS, RAG_INDEXING, EXPORTS]
        expectedTranscriptVersion: {type: integer, minimum: 0}

    TranscriptResponse:
      type: object
      required: [success, data, error, requestId, traceId]
      properties:
        success: {type: boolean}
        data:
          type: object
          required: [meetingId, transcriptVersion, staleStatus, segments]
          properties:
            meetingId: {type: string}
            transcriptVersion: {type: integer}
            staleStatus: {type: string}
            segments:
              type: array
              items:
                $ref: '#/components/schemas/TranscriptSegment'
        error:
          anyOf:
            - $ref: '#/components/schemas/ErrorInfo'
            - type: 'null'
        requestId: {type: string}
        traceId: {type: string}

    TranscriptSegment:
      type: object
      required: [segmentId, startMs, endMs, speakerLabel, originalText, currentText, asrConfidence, timestampPrecision]
      properties:
        segmentId: {type: string}
        startMs: {type: integer}
        endMs: {type: integer}
        speakerLabel: {type: string}
        speakerDisplayName: {type: [string, 'null']}
        originalText: {type: string}
        editedText: {type: [string, 'null']}
        currentText: {type: string}
        asrConfidence: {type: number, minimum: 0, maximum: 1}
        diarizationConfidence: {type: [number, 'null'], minimum: 0, maximum: 1}
        timestampPrecision:
          type: string
          enum: [WORD, SEGMENT, APPROXIMATE]

    # ── Speakers ──────────────────────────────────────────────
    CreateSpeakerProfileRequest:
      type: object
      required: [personId, displayName, consentReference]
      properties:
        personId: {type: string}
        displayName: {type: string}
        consentReference: {type: string}
        authorizedScope:
          type: array
          items: {type: string}

    CreateSpeakerEnrollmentRequest:
      type: object
      required: [audioFileId, consentReference]
      properties:
        audioFileId: {type: string}
        consentReference: {type: string}
        language: {type: [string, 'null']}

    RevokeSpeakerProfileRequest:
      type: object
      required: [reason]
      properties:
        reason: {type: string, minLength: 1}

    ConfirmMeetingSpeakerRequest:
      type: object
      required: [personId, speakerProfileId, expectedTranscriptVersion]
      properties:
        personId: {type: string}
        speakerProfileId: {type: string}
        expectedTranscriptVersion: {type: integer, minimum: 0}

    RejectMeetingSpeakerRequest:
      type: object
      required: [reason]
      properties:
        reason: {type: string}
        candidatePersonId: {type: [string, 'null']}
        candidateSpeakerProfileId: {type: [string, 'null']}

    SpeakerProfileListResponse:
      type: object
      required: [success, data, error, requestId, traceId]
      properties:
        success: {type: boolean}
        data:
          type: object
          required: [items, page]
          properties:
            items:
              type: array
              items:
                $ref: '#/components/schemas/SpeakerProfile'
            page:
              $ref: '#/components/schemas/PageInfo'
        error:
          anyOf:
            - $ref: '#/components/schemas/ErrorInfo'
            - type: 'null'
        requestId: {type: string}
        traceId: {type: string}

    SpeakerProfile:
      type: object
      required: [speakerProfileId, personId, displayName, status]
      properties:
        speakerProfileId: {type: string}
        personId: {type: string}
        displayName: {type: string}
        status: {type: string}
        enrollmentCount: {type: integer}
        lastEnrolledAt: {type: [string, 'null'], format: date-time}

    MeetingSpeakerListResponse:
      type: object
      required: [success, data, error, requestId, traceId]
      properties:
        success: {type: boolean}
        data:
          type: object
          required: [meetingId, speakers]
          properties:
            meetingId: {type: string}
            speakers:
              type: array
              items:
                $ref: '#/components/schemas/MeetingSpeaker'
        error:
          anyOf:
            - $ref: '#/components/schemas/ErrorInfo'
            - type: 'null'
        requestId: {type: string}
        traceId: {type: string}

    MeetingSpeaker:
      type: object
      required: [speakerLabel, confirmationStatus, candidates]
      properties:
        speakerLabel: {type: string}
        displayName: {type: [string, 'null']}
        personId: {type: [string, 'null']}
        speakerProfileId: {type: [string, 'null']}
        confirmationStatus: {type: string}
        candidates:
          type: array
          items:
            type: object
            required: [personId, speakerProfileId, displayName, confidence]
            properties:
              personId: {type: string}
              speakerProfileId: {type: string}
              displayName: {type: string}
              confidence: {type: number, minimum: 0, maximum: 1}

    # ── Minutes ────────────────────────────────────────────────
    RegenerateMinutesRequest:
      type: object
      required: [expectedTranscriptVersion]
      properties:
        expectedTranscriptVersion: {type: integer, minimum: 0}
        expectedMinutesVersion: {type: [integer, 'null'], minimum: 0}

    MinutesResponse:
      type: object
      required: [success, data, error, requestId, traceId]
      properties:
        success: {type: boolean}
        data:
          type: object
          required: [meetingId, minutesId, minutesVersion, staleStatus, markdown, sections]
          properties:
            meetingId: {type: string}
            minutesId: {type: string}
            minutesVersion: {type: integer}
            staleStatus: {type: string}
            markdown: {type: string}
            sections:
              type: array
              items:
                type: object
                required: [type, title]
                properties:
                  type: {type: string}
                  title: {type: string}
                  items:
                    type: array
                    items:
                      type: object
                      required: [text]
                      properties:
                        text: {type: string}
                        evidence:
                          type: array
                          items:
                            type: object
                            properties:
                              segmentId: {type: string}
                              startMs: {type: integer}
                              endMs: {type: integer}
                              evidenceTextSnapshot: {type: string}
            artifactManifestId: {type: string}
        error:
          anyOf:
            - $ref: '#/components/schemas/ErrorInfo'
            - type: 'null'
        requestId: {type: string}
        traceId: {type: string}

    # ── Items ─────────────────────────────────────────────────
    UpdateActionItemRequest:
      type: object
      minProperties: 1
      properties:
        title: {type: string}
        assigneePersonId: {type: [string, 'null']}
        dueAt: {type: [string, 'null'], format: date-time}
        status: {type: string}
        expectedVersion: {type: integer, minimum: 0}

    # ── Documents ─────────────────────────────────────────────
    CreateDocumentRequest:
      type: object
      description: |
        Register an already-uploaded knowledge-base document file for parsing
        and indexing. The file itself must already exist in object storage
        (referenced by `fileId`); phase-1 does not expose an in-app document
        upload flow analogous to audio's multipart upload — the file is
        ingested out-of-band, the API registers its metadata and triggers
        the parse / chunk / embed pipeline.
      required: [title, fileId, documentType, securityLevel]
      properties:
        title: {type: string, minLength: 1}
        fileId: {type: string, minLength: 1, description: 'TOS / MinIO object key for the source file'}
        documentType:
          type: string
          enum: [PDF, DOCX, MARKDOWN, TXT]
        securityLevel:
          $ref: '#/components/schemas/SecurityLevel'
        contentHash:
          type: [string, 'null']
          description: 'SHA-256 of the source file content (optional, recorded for audit / dedup)'

    Document:
      type: object
      description: |
        A registered knowledge-base document. `textExtractionStatus` reflects
        the document-parser pipeline (Apache Tika) state; `status` is the
        business-level lifecycle (ACTIVE / DELETED). RAG retrieval filters
        on both.
      required:
        [documentId, tenantId, title, fileId, documentType, status,
         securityLevel, textExtractionStatus, createdAt, updatedAt]
      properties:
        documentId: {type: string}
        tenantId: {type: string}
        title: {type: string}
        fileId: {type: string}
        documentType:
          type: string
          enum: [PDF, DOCX, MARKDOWN, TXT]
        status:
          type: string
          enum: [ACTIVE, DELETED]
        securityLevel:
          $ref: '#/components/schemas/SecurityLevel'
        textExtractionStatus:
          type: string
          enum: [PENDING, EXTRACTED, FAILED, OCR_UNSUPPORTED, TYPE_UNSUPPORTED]
        contentHash: {type: [string, 'null']}
        sourceUri: {type: [string, 'null']}
        createdAt: {type: string, format: date-time}
        updatedAt: {type: string, format: date-time}
        deletedAt: {type: [string, 'null'], format: date-time}

    DocumentResponse:
      type: object
      required: [success, data, error, requestId, traceId]
      properties:
        success: {type: boolean}
        data:
          $ref: '#/components/schemas/Document'
        error:
          anyOf:
            - $ref: '#/components/schemas/ErrorInfo'
            - type: 'null'
        requestId: {type: string}
        traceId: {type: string}

    DocumentListResponse:
      type: object
      required: [success, data, error, requestId, traceId]
      properties:
        success: {type: boolean}
        data:
          type: array
          items:
            $ref: '#/components/schemas/Document'
        error:
          anyOf:
            - $ref: '#/components/schemas/ErrorInfo'
            - type: 'null'
        requestId: {type: string}
        traceId: {type: string}

    # ── RAG ────────────────────────────────────────────────────
    RagQueryRequest:
      type: object
      required: [question, scope]
      properties:
        question: {type: string, minLength: 1}
        scope:
          type: object
          properties:
            meetingIds:
              type: array
              items: {type: string}
            documentIds:
              type: array
              items: {type: string}
        topN: {type: integer, minimum: 1, maximum: 20, default: 8}
        includeStale: {type: boolean, default: false}

    RagAnswerResponse:
      type: object
      required: [success, data, error, requestId, traceId]
      properties:
        success: {type: boolean}
        data:
          $ref: '#/components/schemas/RagAnswerDTO'
        error:
          anyOf:
            - $ref: '#/components/schemas/ErrorInfo'
            - type: 'null'
        requestId: {type: string}
        traceId: {type: string}

    RagAnswerDTO:
      type: object
      required: [answer, citations, coverage, artifactManifestId]
      properties:
        answer: {type: string}
        citations:
          type: array
          items:
            anyOf:
              - $ref: '#/components/schemas/MeetingSegmentCitation'
              - $ref: '#/components/schemas/DocumentChunkCitation'
        coverage:
          $ref: '#/components/schemas/RagAnswerCoverage'
        artifactManifestId: {type: string}

    MeetingSegmentCitation:
      type: object
      required: [type, meetingId, meetingTitle, segmentId, speaker, startMs, endMs, content]
      properties:
        type: {type: string, const: MEETING_SEGMENT}
        meetingId: {type: string}
        meetingTitle: {type: string}
        segmentId: {type: string}
        speaker: {type: string}
        startMs: {type: integer}
        endMs: {type: integer}
        content: {type: string}

    DocumentChunkCitation:
      type: object
      required: [type, documentId, documentTitle, chunkId, page, content]
      properties:
        type: {type: string, const: DOCUMENT_CHUNK}
        documentId: {type: string}
        documentTitle: {type: string}
        chunkId: {type: string}
        page: {type: integer}
        content: {type: string}

    # ── Export ─────────────────────────────────────────────────
    CreateExportRequest:
      type: object
      required: [format, expectedTranscriptVersion]
      properties:
        format:
          type: string
          enum: [MARKDOWN, DOCX, PDF]
        expectedTranscriptVersion: {type: integer, minimum: 0}
        expectedMinutesVersion: {type: [integer, 'null'], minimum: 0}
        includeTranscript: {type: boolean, default: true}
        includeMinutes: {type: boolean, default: true}
        includeItems: {type: boolean, default: true}

    ExportJobResponse:
      type: object
      required: [success, data, error, requestId, traceId]
      properties:
        success: {type: boolean}
        data:
          type: object
          required: [exportId, meetingId, status, format]
          properties:
            exportId: {type: string}
            meetingId: {type: string}
            status: {type: string}
            format:
              type: string
              enum: [MARKDOWN, DOCX, PDF]
            downloadUrl: {type: [string, 'null']}
            downloadUrlExpiresAt: {type: [string, 'null'], format: date-time}
            sha256: {type: [string, 'null']}
            revoked: {type: boolean}
            expiresAt: {type: string, format: date-time}
        error:
          anyOf:
            - $ref: '#/components/schemas/ErrorInfo'
            - type: 'null'
        requestId: {type: string}
        traceId: {type: string}

    # ── Compliance / Admin Requests ───────────────────────────
    CreateLegalHoldRequest:
      type: object
      required: [reason, scopes]
      properties:
        reason: {type: string, minLength: 1}
        expiresAt: {type: [string, 'null'], format: date-time}
        scopes:
          type: array
          minItems: 1
          items:
            $ref: '#/components/schemas/ComplianceScope'

    ReleaseLegalHoldRequest:
      type: object
      required: [reason]
      properties:
        reason: {type: string, minLength: 1}

    CreateDeletionJobRequest:
      type: object
      required: [reason, scopes]
      properties:
        reason: {type: string, minLength: 1}
        scopes:
          type: array
          minItems: 1
          items:
            $ref: '#/components/schemas/ComplianceScope'

    CreateBreakGlassRequest:
      type: object
      required: [reason, scopes, expiresAt]
      properties:
        reason: {type: string, minLength: 1}
        expiresAt: {type: string, format: date-time}
        scopes:
          type: array
          minItems: 1
          items:
            $ref: '#/components/schemas/ComplianceScope'

    ApproveBreakGlassRequest:
      type: object
      properties:
        comment: {type: [string, 'null']}

    RejectBreakGlassRequest:
      type: object
      required: [reason]
      properties:
        reason: {type: string, minLength: 1}

    LegalHoldResponse:
      type: object
      required: [success, data, error, requestId, traceId]
      properties:
        success: {type: boolean}
        data:
          $ref: '#/components/schemas/LegalHold'
        error:
          anyOf:
            - $ref: '#/components/schemas/ErrorInfo'
            - type: 'null'
        requestId: {type: string}
        traceId: {type: string}

    LegalHold:
      type: object
      required: [legalHoldId, tenantId, name, scope, status, createdAt]
      properties:
        legalHoldId: {type: string}
        tenantId: {type: string}
        name: {type: string}
        scope: {type: string}
        status: {type: string}
        reason: {type: [string, 'null']}
        createdBy: {type: [string, 'null']}
        createdAt: {type: string, format: date-time}
        expiresAt: {type: [string, 'null'], format: date-time}
        releasedAt: {type: [string, 'null'], format: date-time}

    ComplianceScope:
      type: object
      required: [entityType, entityId]
      properties:
        entityType:
          type: string
          enum: [MEETING, MEETING_FILE, DOCUMENT, EXPORT, ARTIFACT, SPEAKER_PROFILE, AUDIT_EVENT]
        entityId: {type: string}

    # ── Delete Requests ────────────────────────────────────────
    DeleteMeetingRequest:
      type: object
      required: [reason]
      properties:
        reason:
          type: string
          minLength: 1
        legalHoldAcknowledged:
          type: boolean
          default: false
        expectedVersion:
          type: integer
          minimum: 0

    DeleteSpeakerProfileRequest:
      type: object
      required: [reason]
      properties:
        reason:
          type: string
          minLength: 1

    # ── Enums (fact source: schemas/common/enums.yaml) ──────────
    SecurityLevel:
      type: string
      enum: [PUBLIC, INTERNAL, CONFIDENTIAL, SECRET]

    MeetingStatus:
      type: string
      enum: [CREATED, PROCESSING, SUCCEEDED, FAILED, DELETED]

    ProcessingTaskStatus:
      type: string
      enum: [PENDING, QUEUED, RUNNING, ORPHANED, PARTIAL_SUCCEEDED, SUCCEEDED, FAILED, CANCEL_PENDING, CANCELLED]

    ProcessingTaskPhase:
      type: string
      enum: [WORKER_DAG_RUNNING, WORKER_DAG_DONE, JAVA_LLM_RUNNING, TERMINAL]

    StepStatus:
      type: string
      enum: [PENDING, QUEUED, RUNNING, SUCCEEDED, FAILED, SKIPPED, CANCELLED]

    AudioUploadStatus:
      type: string
      enum: [INITIATED, UPLOADING, COMPLETED, ABORTED, EXPIRED]

    ProcessingStep:
      type: string
      enum: [AUDIO_UPLOAD, AUDIO_PREPROCESS, ASR, ALIGNMENT, DIARIZATION, SPEAKER_EMBEDDING, SPEAKER_MATCHING, TRANSCRIPT_MERGE, SUMMARY, EXTRACTION, RAG_INDEXING, EXPORT]

    ProcessingStepUpdateSource:
      type: string
      enum: [JAVA_TASK_SERVICE, AI_WORKER_CALLBACK]

    RagAnswerCoverage:
      type: string
      enum: [TRANSCRIPT_ONLY, FULL]
