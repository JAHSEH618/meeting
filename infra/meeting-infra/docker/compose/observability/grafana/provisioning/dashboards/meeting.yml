apiVersion: 1

providers:
  - name: meeting-local
    orgId: 1
    folder: Meeting
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
