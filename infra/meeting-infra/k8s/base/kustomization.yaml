apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Base bundle — overlays inherit + patch.
resources:
  - meeting-api/deployment.yaml
  - meeting-api/service.yaml
  - meeting-web/deployment.yaml
  - ai-worker/statefulset.yaml
