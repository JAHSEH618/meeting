package com.meeting.api.client.enums;

/**
 * Export output format — single source of truth:
 * packages/meeting-contracts/schemas/common/enums.yaml.
 */
public enum ExportFormat {
    MARKDOWN,
    DOCX,
    PDF
}
